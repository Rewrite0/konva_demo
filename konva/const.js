/**
 * 资源路径
 */
const resPath = '/res';

const container = {
  width: 1200,
  height: 846,
};

/**
 * 按钮颜色
 */
const buttonColor = {
  normal: '#383e4a',
  confirm: '#3871e3',
};

/**
 * 按钮样式
 */
const buttonStyle = {
  width: 180,
  height: 64,
  fill: buttonColor.normal,
  cornerRadius: 9,
};

export { resPath, buttonColor, buttonStyle, container };
